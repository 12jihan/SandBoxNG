{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build and debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/${workspaceFolderBasename}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "terminal": "integrated",
      "preLaunchTask": "CMake: Build"
    }
  ]
}
